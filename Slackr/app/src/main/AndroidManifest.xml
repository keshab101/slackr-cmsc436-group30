<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.slackr">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- adding activities in order -->
        <activity android:name=".MainActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HomePage" />
        <activity android:name=".GroupViewActivity" />
        <activity android:name=".GroupPostsActivity" />
        <activity android:name=".GroupPostCreateActivity" />
        <activity android:name=".GroupResultViewActivity" />

        <!-- For Search Activities -->
        <activity android:name=".searchGroupTypes.SearchDayTime" />
        <activity android:name=".searchGroupTypes.SearchEnvironment" />
        <activity android:name=".searchGroupTypes.SearchLearningType" />
        <activity android:name=".searchGroupTypes.SearchSubject"/>
        <activity android:name=".searchGroupTypes.SearchWeekly" />
        <activity android:name=".searchGroupTypes.SearchActivity" />


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>